<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Tempest.Domain\Tempest.Domain.csproj" />
  </ItemGroup>

  <ItemGroup>
    <!-- Fichier unique partage avec Tempest.SampleTarget : un desaccord de contrat echoue a la
         compilation, pas a l'execution. GrpcServices="Both" (pas seulement "Client") : le cote
         serveur genere ici sert de base au faux service gRPC de Tempest.UnitTests — sans lui,
         regenerer les memes messages depuis Tempest.UnitTests entrerait en conflit de type
         (CS0436) avec ceux deja references via ce projet. -->
    <Protobuf Include="..\..\protos\tempest_echo.proto" GrpcServices="Both" Link="Protos\tempest_echo.proto" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Bogus" Version="35.6.5" />
    <PackageReference Include="Google.Protobuf" Version="3.35.1" />
    <PackageReference Include="Grpc.Net.Client" Version="2.83.0" />
    <PackageReference Include="Grpc.Tools" Version="2.83.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.CodeAnalysis.CSharp.Scripting" Version="4.14.0" />
    <PackageReference Include="YamlDotNet" Version="18.1.0" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <!-- Paquet NuGet bibliotheque (roadmap phase 1, dernier bullet) : scenarios de reference
       (DynamicCheckoutWorkflow, WebSocketEchoWorkflow, les quatre workflows gRPC) et le format
       declaratif (DeclarativeWorkflow, ScenarioDefinitionLoader, ExtractionRule) — reutilisables
       tels quels ou comme modele pour ecrire son propre IWorkflow. -->
  <PropertyGroup>
    <PackageId>Tempest.Scenarios</PackageId>
    <Description>Scenarios de reference et format declaratif de Tempest (DynamicCheckoutWorkflow, workflows gRPC/WebSocket, DeclarativeWorkflow) — a reutiliser ou comme modele pour ecrire son propre IWorkflow.</Description>
    <PackageTags>load-testing;performance</PackageTags>
    <PackageReadmeFile>README.md</PackageReadmeFile>
  </PropertyGroup>

  <ItemGroup>
    <None Include="README.md" Pack="true" PackagePath="\" />
  </ItemGroup>

</Project>
