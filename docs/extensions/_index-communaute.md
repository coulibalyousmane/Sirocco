<!-- Genere par tools/Sirocco.ExtensionIndex, ne pas editer a la main. -->

**Aucun paquet ne porte l'étiquette `sirocco-extension` sur nuget.org à ce jour (2026-08-26).**

Ce n'est pas une anomalie de l'index : les extensions de référence de ce dépôt sont
empaquetables mais ne partiront sur nuget.org qu'au premier tag `vX.Y.Z`, et aucune
extension tierce n'existe encore. L'index dira le contraire dès que ce sera le cas.
